# Microsoft Store Listing Draft

Product name:

```text
HDR SDR Brightness Assistant
```

Short title:

```text
HDR SDR Brightness
```

## en-US

Short description:

```text
Keep Windows SDR content brightness predictable when HDR is enabled. Set separate day and night SDR brightness levels, follow Windows Night Light, and run quietly from the system tray.
```

Description:

```text
HDR SDR Brightness Assistant is a lightweight Windows tray utility for keeping SDR content brightness comfortable and predictable while Windows HDR is enabled.

It is built for OLED, QD-OLED, MiniLED, HDR monitors, HDR TVs, and HDR laptop panels where regular SDR apps may look too bright, too dim, or washed out after HDR is turned on.

Set separate day and night SDR content brightness levels, choose whether to follow Windows Night Light, and let the app quietly restore your preferred SDR brightness when a manual change is detected.

The app stays out of the way in the system tray, supports tray-only startup, follows the Windows light or dark app theme, and includes a live SDR/HDR preview in the settings window.

HDR SDR Brightness Assistant is a paid Microsoft Store app with a time-limited free trial. It has no in-app purchases, donation prompts, subscriptions, or unlock codes. It does not install drivers, services, browser components, or secondary software. It stores settings locally for the current Windows user and does not require an account.
```

Product features:

```text
Adjusts Windows SDR content brightness while HDR is enabled
Separate day and night SDR brightness levels
Can follow Windows Night Light
Built-in fallback schedule for day and night switching
Restores configured brightness after manual changes
Live SDR/HDR preview in the settings window
Runs quietly from the system tray
Supports tray-only startup with user consent
Follows Windows light and dark app theme
Paid app with no in-app purchases or subscriptions
7-day full-feature free trial
No account, telemetry, driver, service, or admin requirement
```

Keywords:

```text
HDR SDR brightness
SDR content brightness
Windows HDR brightness
HDR washed out
OLED HDR
Night Light SDR
SDR white level
```

What's new in this version:

```text
Improves HDR screenshot capture responsiveness: region selection is single-instance, stays on top reliably, supports immediate Esc cancel, and drags more smoothly.
Starts the screenshot helper only when capture is used, keeps it warm briefly for repeated screenshots, and exits it after about 90 seconds idle to preserve low background memory usage.
Fixes custom screenshot hotkeys so saved shortcuts echo correctly and remain active after Apply, OK, and restart.
Fixes trial-expired Store background startup behavior so the app no longer keeps auto-launching after the free trial is no longer active.
```

Additional system requirements:

```text
Windows 10 or Windows 11 desktop PC
HDR-capable display recommended
Windows HDR must be enabled to adjust SDR content brightness
```

## zh-CN

Short description:

```text
开启 Windows HDR 时，让 SDR 内容亮度保持舒适稳定。支持白天/夜间两组亮度、跟随 Windows 夜间模式，并可安静常驻系统托盘。
```

Description:

```text
HDR SDR 亮度助手是一个轻量的 Windows 托盘工具，用来在开启 Windows HDR 时，让 SDR 内容亮度保持稳定、舒适、可预期。

它适合 OLED、QD-OLED、MiniLED、HDR 显示器、HDR 电视和支持 HDR 的笔记本屏幕。开启 HDR 后，如果普通 SDR 软件看起来过亮、过暗或发灰，可以用它自动切换和修正 SDR 内容亮度。

你可以分别设置白天和夜间 SDR 内容亮度，选择是否跟随 Windows 夜间模式，也可以使用内置默认时段。检测到手动修改 SDR 内容亮度后，应用可以自动恢复到配置值。

应用会安静常驻系统托盘，支持经用户开启后的托盘模式自启动，自动跟随 Windows 浅色/深色应用主题，并在设置页提供 SDR/HDR 实时预览。

HDR SDR 亮度助手是 Microsoft Store 付费应用，并提供限时免费试用。它不包含应用内购买、捐赠提示、订阅或解锁码，不安装驱动、服务、浏览器组件或附加软件。配置仅保存在当前 Windows 用户本地，不需要账户登录。
```

Product features:

```text
HDR 开启时调整 Windows SDR 内容亮度
支持白天和夜间两组 SDR 亮度
可跟随 Windows 夜间模式
内置白天/夜间默认时段
检测手动修改后可自动恢复配置亮度
设置页提供 SDR/HDR 实时预览
安静常驻系统托盘
支持经用户同意后的托盘模式自启动
自动跟随 Windows 浅色/深色应用主题
付费应用，无应用内购买或订阅
7 天完整功能免费试用
无需账户、遥测、驱动、服务或管理员权限
```

Keywords:

```text
HDR SDR 亮度
SDR 内容亮度
Windows HDR 亮度
HDR 发灰
OLED HDR
夜间模式 SDR
SDR 白点
```

What's new in this version:

```text
继续优化 HDR 截图体验：区域截图遮罩保持单例、可靠置顶，Esc 可立即取消，鼠标框选更顺滑。
截图 helper 改为按需启动，短时间内连续截图会保温复用，空闲约 90 秒后自动退出，避免常驻占用后台内存。
修复自定义截图快捷键保存后回显不正确、应用或重启后可能回到旧值的问题。
修复 Microsoft Store 试用结束后后台自启仍会尝试拉起程序的问题。
```

Additional system requirements:

```text
Windows 10 或 Windows 11 桌面电脑
建议使用支持 HDR 的显示器
需要开启 Windows HDR 后才能调整 SDR 内容亮度
```

## Assets To Prepare

Screenshots:

```text
en-US/settings-light-1366x768.png
en-US/settings-dark-1366x768.png
en-US/tray-menu-1366x768.png
en-US/hdr-preview-controls-1366x768.png
zh-CN/settings-light-1366x768.png
zh-CN/settings-dark-1366x768.png
zh-CN/tray-menu-1366x768.png
zh-CN/hdr-preview-controls-1366x768.png
Windows Startup settings state, if needed for certification evidence
```

Required URLs:

```text
Privacy policy URL: https://github.com/yinjunonly/hdr-sdr-brightness/blob/main/docs/privacy-policy.md
Support URL: https://github.com/yinjunonly/hdr-sdr-brightness/blob/main/docs/support.md
Website URL: optional
```

Store properties to confirm:

```text
Category: Utilities & tools
Pricing: Paid
Base price: low-volume tier, target the tier closest to US$1.99
Launch sale: yes, 50% off for 14 days, offered to everyone, target the tier closest to US$0.99
Free trial: yes, time-limited 7-day trial with full functionality
Markets: all possible markets, including China, and include future markets when available
Age rating: complete the Partner Center age rating questionnaire as a non-game utility; expected lowest/general audience rating
Privacy policy: required before submission
Support contact: required before submission
```
